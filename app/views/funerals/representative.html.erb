<div class="container">
  <div class="card-container unaware_state">
    <div class="card-contents unaware_state">
      <p><i class="bi bi-person-circle"></i> <%= Funeral::QUESTIONS[:representative] %> <i class="bi bi-person-circle"></i></p>
      <div id="card-contents-btn">
        <%= simple_form_for(@funeral, :url => wizard_path, :method => :put) do |f| %>
          <%= f.input :representative, as: :hidden, input_html: { value: true} %>
          <%= f.submit 'Yes', class: 'btn btn-primary' %>
        <% end %>
        <%= simple_form_for(@funeral, :url => wizard_path, :method => :put) do |f| %>
          <%= f.input :representative, as: :hidden, input_html: { value: false} %>
          <%= f.submit 'No', class: 'btn btn-primary' %>
        <% end %>
      </div>
    </div>
  </div>
  <div class="flash flash-success alert alert-dismissible fade show" role="alert">
    <span><strong>➡️</strong> Last one for now! 👍 Nice work! It's just a "yes" or "no" this time but consider before you click.</span>
    <a data-bs-dismiss="alert" aria-label="Close">
      <i class="fas fa-times"></i>
    </a>
  </div>
  <div class="flash flash-success alert alert-dismissible fade show" role="alert">
    <span><strong>👉🏻</strong> 🎉 One tissue donor – someone who can donate bone, tendons, cartilage, connective tissue, skin, corneas, sclera, and heart valves and vessels – can impact the lives of as many as 75 people.
    </span>
    <a data-bs-dismiss="alert" aria-label="Close">
      <i class="fas fa-times"></i>
    </a>
  </div>
</div>
